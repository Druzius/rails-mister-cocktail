<h1><%= @cocktail.name %></h1>



<p>Cocktail name: <%= @cocktail.name %></p>
<p>Ingredients:</p>
<% @cocktail.doses.each do |dose| %>
<p><%= dose.description %> - <%= dose.ingredient.name %> <%= link_to '', dose_path(dose), method: :delete, data:  { confirm: "Are you sure?" }, class: "fa fa-trash" %>
<% end  %>



<% if @cocktail.doses.empty? %>
  <p><%= link_to 'Add an ingredient', new_cocktail_dose_path(@cocktail) %></p>
<% else %>
  <p><%= link_to 'Add another ingredient', new_cocktail_dose_path(@cocktail) %></p>
<% end %>

<p><%= link_to 'Back to our cocktail list', cocktails_path %></p>

